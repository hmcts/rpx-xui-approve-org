import {OrgOverviewComponent} from './org-pending-overview.component';

describe('Org Overview Component', () => {
    it('should have a component', () => {
        const component = new OrgOverviewComponent();
        expect(component).toBeTruthy();
});
});

