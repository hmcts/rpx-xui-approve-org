import { DeleteOrganisationSuccessComponent } from './delete-organisation-success.component';

// TODO check why this is throwing errors for
xdescribe('Org Approval Success Component', () => {
    it('should have a component', () => {
        const component = new DeleteOrganisationSuccessComponent();
        expect(component).toBeTruthy();
});
});
