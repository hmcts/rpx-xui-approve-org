import { OrgApprovalSuccessComponent } from './org-approval-success.component';

describe('Org Approval Success Component', () => {
    it('should have a component', () => {
        const component = new OrgApprovalSuccessComponent();
        expect(component).toBeTruthy();
});
});
