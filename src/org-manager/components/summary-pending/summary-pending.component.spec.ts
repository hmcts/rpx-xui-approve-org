import { SummaryPendingComponent } from './summary-pending.component';

describe('Summary Component', () => {
    it('should have a component', () => {
        const component = new SummaryPendingComponent();
        expect(component).toBeTruthy();
});
});
