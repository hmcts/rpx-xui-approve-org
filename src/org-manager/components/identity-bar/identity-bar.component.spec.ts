import { IdentityBarComponent } from './identity-bar.component';

describe('Identity Bar Component', () => {
    it('should have a component', () => {
        const component = new IdentityBarComponent();
        expect(component).toBeTruthy();
});
});
